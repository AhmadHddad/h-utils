/**
 * @description A no-operation function that returns undefined regardless of the arguments it receives.
 * @example noop() =>  undefined;
 */
export default function noop(..._: any[]): void {}
  
